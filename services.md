# Services used by heroku-buildpack-submodule

> **Status: INCOMPLETE (stub, 2026-09-24).** Created so every repo has one. Fill in the TODOs; see the fable repo's `services.md` for a complete example.

Every external service or local infrastructure piece this project uses: what uses it, config/env vars (names only), docs, whether and how it runs locally, and every way we access it (API, CLI, MCP, Chrome), easiest and cheapest first.

Detected automatically from package.json (verify and complete):


| Service | Used by | Local | Configuration (env var names) | Docs | How we access it (easiest first; Chrome last) |
| --- | --- | --- | --- | --- | --- |
| TODO | | | | | |

## Hosting, build and release

TODO: where it runs (Vercel / Heroku / Railway / Coolify / app stores), CI workflows, secrets locations (names only).
